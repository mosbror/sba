# SBA frontend

Nuxt 3 touch-first frontend for the SBA inspection app.

## Commands

```bash
npm install
npm run dev
npm run build
npm run preview
```

The API base is read from `NUXT_PUBLIC_API_BASE` and defaults to `/api` in code.
